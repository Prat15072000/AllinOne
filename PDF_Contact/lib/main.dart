import 'package:flutter/material.dart';
import 'package:pdf/ui_task.dart';
import 'contact_form.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Home(),
    );
  }
}
